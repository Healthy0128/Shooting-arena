# Local models

Place project-owned or redistributable GLB/GLTF assets here. Recommended folders:

- `characters/`
- `weapons/`
- `stage/`

The public build no longer depends on deploy-time patch scripts. Third-party KayKit assets currently referenced by remote URLs can be migrated here incrementally.
