name: Deploy Duel Arena to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Materialize v6.7.3 horizontal battle axis
        if: ${{ !contains(github.event.head_commit.message || '', '[materialized-v673]') }}
        run: |
          python materialize_v673.py
          node --check src/main.js
          grep -q 'v6.7.3' index.html
          grep -q 'HORIZONTAL BATTLE AXIS' index.html
          grep -q 'root.position.set(i?4.5:-4.5,0,0)' src/main.js
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/main.js index.html
          if ! git diff --cached --quiet; then
            git commit -m 'Materialize v6.7.3 horizontal battle axis [materialized-v673]'
            git push origin HEAD:main
          fi

      - name: Syntax check
        run: node --check src/main.js

      - name: Prepare direct site
        run: |
          mkdir -p _site
          rsync -a \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '_site' \
            --exclude 'deploy' \
            --exclude 'materialize_v673.py' \
            ./ _site/
          touch _site/.nojekyll
          test -s _site/index.html
          test -s _site/style.css
          test -s _site/src/main.js

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
